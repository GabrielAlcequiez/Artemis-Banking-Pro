@model ABP.WebApp.Areas.Cashier.ViewModels.Accounts.CashierWithdrawalConfirmationViewModel
@{
    ViewData["Title"] = "Confirmar retiro";
}

<div class="mx-auto max-w-3xl space-y-6">
    <header>
        <a class="text-sm text-ink-muted hover:text-ink" asp-action="Withdrawal">← Cancelar y volver</a>
        <h1 class="mt-3 text-2xl font-semibold text-ink">Confirmar retiro</h1>
        <p class="mt-1 text-sm text-ink-muted">¿Está seguro que desea realizar este retiro?</p>
    </header>

    <section class="card p-6">
        <dl class="grid gap-5 text-sm sm:grid-cols-2">
            <div>
                <dt class="text-ink-muted">Titular de la cuenta</dt>
                <dd class="mt-1 font-medium text-ink">@Model.AccountOwnerFullName</dd>
            </div>
            <div>
                <dt class="text-ink-muted">Cuenta origen</dt>
                <dd class="mt-1 font-mono text-ink">@Model.AccountNumber</dd>
            </div>
            <div>
                <dt class="text-ink-muted">Saldo disponible</dt>
                <dd class="tnum mt-1 font-medium text-ink">RD$ @Model.AvailableBalance.ToString("N2")</dd>
            </div>
            <div>
                <dt class="text-ink-muted">Monto a retirar</dt>
                <dd class="tnum mt-1 font-medium text-ink">RD$ @Model.Amount.ToString("N2")</dd>
            </div>
        </dl>
    </section>

    <form asp-action="ExecuteWithdrawal" method="post" class="flex gap-3">
        @Html.AntiForgeryToken()
        <input asp-for="AccountNumber" type="hidden" />
        <input asp-for="Amount" type="hidden" />
        <a class="btn btn-ghost border border-border" asp-action="Withdrawal">Cancelar</a>
        <button type="submit" class="btn btn-primary">Confirmar retiro</button>
    </form>
</div>
